class Car{
  /* const Car(this.name,this.imageUrl);
  //{} 代表可选的参数
  // final String? name; ? 空 安全的 / 添加默认值
  */
  const Car({this.name,this.imageUrl});
  final String? name;
  final String? imageUrl;
}